// Código não executável!

// Procedural
processamento (valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        //...
    }
}

objeto.processamento() // foco passou a ser o objeto

// PRINCIPIOS IMPORTANTES DA OO:
//1. Abstração => como traduzir um objeto do mundo real para o seu sistema!
//2. Encapsulamneto => detalhes de implementação do sistemas que devem estar escondidos, deixando disponível apenas o que será necessário ao usuário, proporcionando uma interface de comuncicação fácil ao usuário final.
//3. Herança (prototype)=> A forma como um objeto recebe atributos e comportamentos de um obj "Pai"
//4. Polimorfismo => Múltiplas formas, permite que a partir que um objeto genérico possa ter vários outros objetos atribuidos a ele.
